import React from 'react';

const App: React.FC = () => {
  return (
    <div>
      <h1>Hello, Rails with React and TypeScript!</h1>
      <p>This is a React component rendered inside a Rails view.</p>
    </div>
  );
};

export default App;
